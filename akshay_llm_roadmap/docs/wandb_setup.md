# W&B Quickstart

1. Create a W&B account at https://wandb.ai
2. Run `wandb login` and paste your API key
3. Run example: `python akshay-llm-roadmap/examples/experiment.py`
4. If you don't want W&B, the example safely runs without it (it checks for wandb import).
